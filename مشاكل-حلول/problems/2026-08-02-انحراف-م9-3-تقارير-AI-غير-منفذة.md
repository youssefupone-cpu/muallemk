# انحراف م9.3 — إضافة "تقارير AI" (نوع report) غير منفذة

- **الحالة**: open
- **النوع**: انحراف مطابقة (ميزة ناقصة من الخطة المعتمدة)
- **التاريخ**: 2026-08-02

## الوصف

الخطة `~/.local/share/opencode/plans/PLAN.md` في بند **م9.3 إضافة "تقارير AI"** تنص:
> نوع `report` — مولّد تقارير **عند الطلب** — اختيار كتاب/محادثة → تقرير (ملخص + استشهادات) → تنزيل MD/PDF. ✅ النجاح: تقرير كامل باستشهادات حقيقية.

## الأدلة

- مجلدات الإضافات المثبتة (`backend/plugins/`): `books` (data-source)، `broken-plugin` (tool)، `grades-tool` (tool)، `study-table` (ui-page) — **لا توجد إضافة `report`**.
- `backend/app/plugins/manifest.py:23` يعرّف `PluginType.REPORT = "report"` في السكيما لكن لا مشغّل (entrypoint) ولا صفحة.
- لا إشارة "تقرير/Report" في مخرجات `frontend/src/pages/` (الصفحات: Chat, Documents, Plugins, Settings, StudyTable فقط).
- `grep report backend/app/plugins/router.py` → بلا نتائج (لا endpoint تقارير).

## الأثر

- معيار نجاح م9.3 في الخطة **غير محقق**.
- خطوة "معيار قابلية التوسع" (إضافة إضافتين جديدتين بلا تعديل النواة) لم تختبر عبر إضافة report.

## الحل المقترح

وضعية البناء: إنشاء إضافة `report` من نوع `report` (entrypoint يولد ملخص + استشهادات حقيقية عبر RAG) + صفحة/أداة اختيار كتاب/محادثة → تنزيل MD (PDF اختياري)، أو — إذا قرر المستخدم إسقاطها من النطاق — تحديث الخطة حصراً.