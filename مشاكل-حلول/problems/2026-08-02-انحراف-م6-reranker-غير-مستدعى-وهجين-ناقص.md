# انحراف م6 (مُحدَّث) — بحث متجهي فقط في /rag/query + لا hybrid؛ الـ rerank يعمل في /ask

- **الحالة**: open (جزئي — قيد المعالجة في وضعية البناء)
- **النوع**: انحراف مطابقة (مكوّن ناقص من م6)
- **التاريخ**: 2026-08-02 (أُنشئ) — 2026-08-02 (تدقيق مُحضّر)

## تصحيح التدقيق (2026-08-02)

النسخة الأولى من هذا الملف ذُكرت «الـ reranker غير مستدعى» — **هذا غير دقيق**: الأدلة تُظهر أن `OllamaReranker` يعمل بنجاح في مسار "اسأل كتابك" (م7):
- `backend/app/rag/router.py:114` — `reranker = OllamaReranker(base_url=settings.ollama_base_url)`
- `backend/app/rag/router.py:130` — يُمرَّر إلى `ask_stream(..., reranker=reranker)`
- `backend/app/rag/ask.py:49-50` — `if reranker: chunks = await reranker.rerank(question, chunks, top_k=top_k)`

## الانحراف الفعلي المتبقي (م6)

- `engine.query()` في `backend/app/rag/engine.py` يستخدم `tbl.search(q_vec)` فقط — **بحث متجهي (vector-only)** بلا sparse/FTS hybrid بينما خطة م6 تنص "استرجاع hybrid".
- مسار `/rag/query` (`router.py:81`) لا يطبّق rerank (خلافاً لـ /rag/ask) — عدم تناسق في الجودة بين المسارين.
- docstring في `engine.py:3` («استرجاع hybrid») لا يطابق الكود الفعلي.

## الأثر

جودة الاسترجاع في `/rag/query` دون مستوى الخطة؛ الفجوة تتفاقم مع زيادة الكتب.

## الحل المقترح (في وضعية البناء)

- إضافة rerank اختياري في `/rag/query` عبر `OllamaReranker` (يتجاهل الفشل بأمان — يبقى vector عند غياب Ollama rerank model).
- تصحيح docstring في `engine.py` لمطابقة الواقع.
- تسجيل الـ hybrid sparse كقرار موقوف (يتطلب فهرس FTS + خوارزمية sparse — بند اختياري لاحق؛ لا يوقف المطابقة).